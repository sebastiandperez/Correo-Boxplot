> **SPRINT1-INTEGRATION-GATE-01: COMPLETE.** A-01→A-08 consumen el Local Engine productivo mediante P-01/P-02/P-03. El smoke A-08 ejecuta Vue → Application → adapters Tauri → IPC → Rust → SQLCipher Development, incluida invalidación post-commit, envío durable sin Email ficticio y reapertura sin servidor remoto.

Epic	ID	Pri.	Título	Descripción	Paths previstos	Dep.	SP	Criterios de aceptación	Tests a añadir
Application State	A-01	P0	Completar API pública de stores	Conservar runtime.ts existente y materializar mail + composer; fijar state/actions públicos	src/app/stores/runtime.ts, mail.ts, composer.ts	Contrato preliminar RR	3	Estados coinciden con application-state.md; cero persistencia; cero JMAP	runtime.test.ts, mail.test.ts, composer.test.ts
Application State	A-02	P0	Composition root de Application	Inyectar ReadRepository; preparar consumo futuro de LocalChangeSource; componentes no conocen adapter	src/app/application.ts o src/app/services/*	B-01	3	Una sola frontera de creación/DI; ningún componente importa Tauri	Unit test con fake repository
Mail Flow	A-03	P0	Mail store local-first	Implementar accounts/mailboxes/window/selected email/body; releer ante invalidación P-03	src/app/stores/mail.ts	A-02, B-01/B-02	5	Toda lectura usa RR; invalidación produce reread; sync-demand no bloquea UI	Selección, pagination, invalidación, errores
Presentation	A-04	P0	Conectar UI Shell	Reemplazar contenido estático de sidebar/list/viewer por proyecciones/actions del store	src/components/layout/*, mailbox/*, message-list/*, message-viewer/*	A-03	5	Selección funciona; vacíos/loading/error claros; cero fake mail	Vue Test Utils para interacciones principales
Security	A-05	P1	Frontera segura del MessageViewer	Sanitización por render, iframe sandbox, bloqueo de remote resources y enlaces controlados	src/security/*, MessageViewer.vue	A-03	5	HTML hostil no ejecuta script/forms ni navega webview	Payloads XSS, javascript URLs, remote image
Composer	A-06	P1	Composer memory-only y queue semantics	Montar Composer; editar to/subject/body; stage SendMutation vía SyncPort; limpiar solo tras commit exitoso	src/app/stores/composer.ts, Composer.vue, AppShell.vue	B-01/B-02	5	Falla del commit conserva texto; éxito limpia; no draft durable	Success/failure queue; discard; no persistence plugin
Runtime UX	A-07	P1	Estados runtime	Proyectar local/auth/connectivity sin mezclar ciclos	UI + runtime.ts	A-01	2	ready + anonymous funciona; offline no se presenta como storage error	Matrix de runtime states
Integration	A-08	P0	Corte local-first A+B	Ejecutar UI real contra TauriReadRepository y DB de test/desarrollo	src/app/*, adapters con B	B-04/B-06	3	Tauri muestra datos locales y responde sin servidor	Smoke local vertical


### SPRINT 2

> **A2-00 — COMPLETE.** Se restauró el contrato aceptado de seguridad del MessageViewer: sandbox exacto `allow-same-origin`, sanitización por render, recursos remotos bloqueados y navegación HTTP/HTTPS exclusivamente mediante el opener controlado. También se corrigió el mojibake introducido en rutas Persona A.
>
> **A2-01 — COMPLETE.** `AccountSetup` y su store Pinia efímero recopilan y validan la configuración **Boxplot Local / IMAP** (`127.0.0.1:1143`, SMTP derivado `1587`). La contraseña permanece solo en memoria. A2-01 no autentica: el bridge real sigue en **A2-02** y el first-run routing sigue en **A2-03**.


| ID                                     | Archivos principales                                                                                                                     | Tarea concreta                                                                                                                                                                                                                                                                 | Impacto                                                                                                                                              |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A2-01 Account Setup**                | **NEW** `src/components/account/AccountSetup.vue`, **NEW** `src/app/stores/account-setup.ts`, `src/App.vue`                              | Crear pantalla de configuración con `usuario`, `contraseña`, `proveedor/protocolo`, `servidor`, `puerto`. Añadir perfil **Boxplot Local / IMAP** con `127.0.0.1:1143`; SMTP 1587 derivado internamente. Password exclusivamente en memoria.                                    | Permite que una instalación limpia deje de mostrar simplemente “No hay cuentas” y pueda conectarse a Alice/Bob. **Desbloquea el primer login real.** |
| **A2-02 Auth Application Bridge**      | `src/app/application.ts`, `src/app/stores/runtime.ts`, `src/app/vue-application-context.ts`                                              | Consumir la interfaz de conexión que entregue C. Mapear `connecting → authenticating`, éxito → `authenticated`, credenciales incorrectas → `anonymous + error`, sesión perdida → `expired`. No importar `ImapAdapter`, SMTP ni JMAP.                                           | Conecta la UI de A con autenticación real manteniendo la frontera protocol-neutral.                                                                  |
| **A2-03 First-run / Shell Routing**    | `src/App.vue`, `src/components/layout/AppShell.vue`, `src/components/account/AccountSetup.vue`                                           | Mostrar `AccountSetup` cuando no hay cuenta/configuración remota utilizable. Mostrar `AppShell` cuando hay cuenta local. Permitir `local=ready + auth=anonymous`: el correo cacheado sigue disponible aunque no exista sesión remota.                                          | Hace coherente el arranque: primera instalación → configurar; reinicio offline → correo local sigue funcionando.                                     |
| **A2-04 Remote Connection Status**     | `src/components/mailbox/MailboxSidebar.vue`, `src/app/stores/runtime.ts`, **opcional NEW** `src/components/account/ConnectionStatus.vue` | Sustituir el estado demasiado genérico actual por estados claros: `Local`, `Conectando…`, `En línea`, `Sin conexión`, `Sesión vencida`, `Error local`. Añadir acción **Reconectar** cuando corresponda.                                                                        | El usuario distingue correctamente “SQLCipher funciona” de “servidor remoto no disponible”.                                                          |
| **A2-05 Manual Refresh**               | `src/components/message-list/MessageList.vue`, `src/app/application.ts`, eventualmente `src/components/mailbox/MailboxSidebar.vue`       | Añadir botón **Actualizar**. Debe llamar al caso de uso de sincronización de C, nunca directamente a IMAP/JMAP. Mostrar `Sincronizando…`; al terminar no insertar datos en Pinia: esperar `C → SyncPort → SQLCipher → P-03 → reread`.                                          | Permite que Bob descubra el correo de Alice **sin IDLE/push**, justo lo que necesita nuestro servidor mínimo.                                        |
| **A2-06 Body-on-demand UX**            | `src/components/message-viewer/MessageViewer.vue`, `src/app/application.ts`, `src/app/stores/mail.ts`                                    | Cuando `readEmailBody()` devuelve `notCached`, solicitar a C la materialización remota del body. Mientras tanto mostrar loading. Cuando C haga `cacheEmailBody()`, P-03 provoca reread y aparece el body. No entregar body remoto directamente a Vue.                          | Permite abrir un Email descargado inicialmente solo como metadata sin romper la arquitectura local-first.                                            |
| **A2-07 Remote Mutation Feedback**     | `src/components/message-list/MessageList.vue`, `src/app/application.ts`, **posible NEW** `src/app/stores/sync-status.ts`                 | Mantener las acciones actuales Seen/Flagged/Trash, pero añadir feedback de `pendiente / sincronizado / error`. No rehacer las mutaciones: actualmente `MessageList` ya llama `toggleKeyword()` y `moveEmail()`, que crean las mutaciones optimistas locales.                   | Cuando C ejecute `UID STORE`/`UID MOVE` o JMAP equivalente, la UX podrá mostrar que el cambio local todavía está pendiente de servidor.              |
| **A2-08 Composer Remote Status**       | `src/components/composer/Composer.vue`, `src/app/stores/composer.ts`, `src/app/services/send-service.ts`                                 | Mantener la semántica actual: botón Send termina cuando `PendingMutation` quedó durable. Añadir feedback como **“En cola” / “Enviando…” / “Enviado” / “Error de sincronización”** según el estado que exponga Outbox. No volver a crear fake Sent.                             | El usuario distingue entre “guardado localmente para enviar” y “servidor realmente aceptó el mensaje”. Fundamental para offline.                     |
| **A2-09 Reconnect / Session Recovery** | `src/components/account/AccountSetup.vue`, `src/app/stores/account-setup.ts`, `src/app/stores/runtime.ts`, `src/App.vue`                 | Al reiniciar, si SQLCipher contiene la cuenta pero la contraseña ya no existe en memoria, permitir **Conectar para sincronizar** sin bloquear Inbox cacheado. Para MVP puede volver a pedir los 4 campos completos. No guardar password ni crear localStorage de credenciales. | Conserva nuestra decisión MVP de password memory-only y demuestra correctamente el modelo local-first.                                               |
| **A2-10 Persona A Remote UX Tests**    | `src/components/__tests__/`, `src/app/__tests__/`, **NEW** `tests/persona-a-remote-integration/` o equivalente                           | Tests de AccountSetup, auth failure/success, cached-mail while anonymous, Refresh, body notCached→materialized, pending send, remote mutation failure y reconnect. No usar adapters concretos en unit tests.                                                                   | Evita que la incorporación de C vuelva a romper A-01→A-08.                                                                                           |
| **A2-11 Alice/Bob UI Acceptance**      | script E2E existente o **NEW** `scripts/run-persona-a-remote-demo.mjs`, documentación `docs/development/`                                | Ejecutar dos perfiles locales independientes: Alice y Bob. Desde UI real: login Alice/Bob → Alice envía → Bob Refresh → Bob abre → Seen → Flagged → Trash → Bob responde → Alice Refresh.                                                                                      | **Gate final de Sprint 2 de A.** Demuestra que la aplicación ya es utilizable como cliente de correo y no solo como shell local.                     |
